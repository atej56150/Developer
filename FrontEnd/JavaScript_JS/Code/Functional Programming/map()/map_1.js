const numbers = [10, 20, 30];

const result = numbers.map((number) => {
  return number * 2;
});

console.log(result);



/*

Output :- [20, 40, 60]
map() creates and returns a new array in JavaScript.

*/
