version: "3.9"

services:
  backend:
    build: ./backend
    container_name: my-backend-container
    ports:
      - "5000:5000"
    restart: always

  frontend:
    build: ./frontend
    container_name: my-frontend-container
    ports:
      - "3000:3000"
    restart: always
